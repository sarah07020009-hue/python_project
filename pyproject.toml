[project]
name = "python-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=3.0.3",
    "pip>=26.1.2",
    "reportlab>=4.5.1",
]

[tool.uv.workspace]
members = [
    "05_23",
]
